---
- name         : Simple Mode (1)
  hosts        : all
  gather_facts : no
  sudo         : True
  vars_files   :
                 - "user_db_1.yml"
  roles        :
                 - ansible-usermanage
