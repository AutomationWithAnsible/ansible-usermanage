---
- name         : Simple Mode (1)
  hosts        : all
  gather_facts : no
  sudo         : True
  connection   : local

  vars_files   :
                 - "simple_user_db.yml"
  roles        :
                 - "ansible-usermanage"
