---
- name         : Group  (1)
  hosts        : all
  gather_facts : no
  sudo         : True
  connection   : local

  vars_files   :
                 - "group_var_group_db.yml"
  roles        :
                 - "ansible-usermanage"
